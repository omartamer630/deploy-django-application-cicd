FROM python:3.11-slim

# Set the working directory
WORKDIR /app

COPY requirements.txt .

# Install dependencies
RUN apt-get update && apt-get install -y \
    && pip install --no-cache-dir -r requriements.txt && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

COPY . .
